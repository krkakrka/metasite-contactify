import React from 'react';
import './Contacts.css';

function Contacts() {
  return (
    <main className="Contacts">
      <div>Filter</div>
      <button>Add new contact</button>
      <div>contacts</div>
    </main>
  );
}

export default Contacts;
